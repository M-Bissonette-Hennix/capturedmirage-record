import test from 'node:test';import assert from 'node:assert/strict';
import {transformLegacyV1Game} from '../src/db.js';
const OLD_START='rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1';
test('actual v0.1 record-foundation game shape transforms to v3 without inventing certification',()=>{const old={id:'legacy-game-0001',schema:'record-game/1',createdAt:'2026-09-01T00:00:00.000Z',updatedAt:'2026-09-01T00:00:00.000Z',state:'CERTIFIED',metadata:{event:'Club',site:'Asheville',date:'2026.09.01',round:'1',board:'3',section:'Open',white:'A',black:'B',result:'1-0',timeControl:'90+30'},startFen:OLD_START,sans:['e4'],plies:[{ply:1,san:'e4',before:OLD_START,after:'rnbqkbnr/pppppppp/8/8/4P3/8/PPPP1PPP/RNBQKBNR b KQkq - 0 1',provenance:'USER_ENTERED',sourceRef:null}],notes:'n',certification:{legacy:true}};const g=transformLegacyV1Game(old);assert.equal(g.schema,'record-game/3');assert.equal(g.certification,null);assert.equal(g.certificationStatus,'INVALIDATED');assert.equal(g.flags.needsReview,true);assert.equal(g.moves.length,1);assert.equal(g.moves[0].san,'e4');});
test('unsupported legacy schema fails closed',()=>assert.throws(()=>transformLegacyV1Game({schema:'record-game/0'}),/unsupported/i));
